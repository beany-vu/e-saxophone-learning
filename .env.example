# Copy to .env and adjust. docker-compose reads these.

# Postgres
POSTGRES_USER=yds
POSTGRES_PASSWORD=yds_dev_pw
POSTGRES_DB=yds120

# API
# JWT signing secret. Generate a real one for anything beyond local dev:
#   openssl rand -base64 48
JWT_SECRET=dev-secret-change-me
